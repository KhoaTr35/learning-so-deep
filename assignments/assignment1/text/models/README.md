# Models

This directory stores trained model weights and serialized models.

## Contents
- Trained model checkpoints (.pt, .pth files)
- Model definitions and architectures
- Weights from best iterations
- Model metadata and configuration

## Notes
- Large model files are typically not committed to git
- Use `.gitignore` to exclude model files
- Consider using Git LFS for large files if needed
